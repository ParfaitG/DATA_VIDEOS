?`dbConnect
``
`
?`dbConnect`
library(RSQLite)
conn <- dbConnect(RSQLite::SQLite(), "/data/BGCI_Plants_ThreatSearch.db")
library(RSQLite)
conn <- dbConnect(RSQLite::SQLite(), "data/BGCI_Plants_ThreatSearch.db")
library(RSQLite)
conn <- dbConnect(RSQLite::SQLite(), "data/BGCI_Plants_ThreatSearch.db")
dbDisconnect(conn)
rm(conn)
library(RSQLite)
conn <- dbConnect(RSQLite::SQLite(), "data/BGCI_Plants_ThreatSearch.db")
76 + 16
library(RSQLite)
conn <- dbConnect(RSQLite::SQLite(), "data/BGCI_Plants_ThreatSearch.db")
library(XML)
doc <- xmlParse("~/Documents/Data/Data_Videos/data/cta_monthly_l_rides.xml")
cta_names <- xpathSApply(doc, "//row", xmlName)
cta_names <- xpathSApply(doc, "//row/*", xmlName)
cta_names <- xpathApply(doc, "//row/*", xmlName)
cta_names <- xpathApply(doc, "//row", xmlName)
cta_names <- lapply(1:33761, function(i) xpathApply(doc, paste0("//row[",i,"]/*", xmlName)
)
)
cta_names <- lapply(1:33761, function(i) xpathApply(doc, paste0("//row[",i,"]/*", xmlName))
cta_names <- lapply(1:33761, function(i) xpathApply(doc, paste0("//row[",i,"]/*", xmlName)))
cta_names <- lapply(1:33761, function(i) xpathApply(doc, paste0("//row[",i,"]/*", xmlName)))
cta_names <- lapply(1:33761, function(i) xpathApply(doc, paste0("//row[",i,"]/*"), xmlName))
cta_names <- lapply(1:330, function(i) xpathApply(doc, paste0("//row[",i,"]/*"), xmlName))
doc <- xmlParse("~/Documents/Data/Data_Videos/data/cta_monthly_l_rides.xml")
cta_names <- lapply(1:330, function(i) xpathApply(doc, paste0("//row[",i,"]/*"), xmlName))
cta_names_test <- xpathApply(doc, "//row[1]/*")
cta_month_rides_df <- xmlToDataFrame(doc, nodes=getNodeSet(doc, "//row[2:33767"))
cta_month_rides_df <- xmlToDataFrame(doc, nodes=getNodeSet(doc, "//row[2:33767]"))
cta_month_rides_df <- xmlToDataFrame(doc, nodes=getNodeSet(doc, "//row[position() > 1]"))
View(cta_month_rides_df)
rm(cta_names, cta_names_test)
gc()
cta_month_rides_df2 <- xmlToDataFrame(doc, nodes=getNodeSet(doc, "//row[1]"))
cta_month_rides_df2 <- xmlToDataFrame(doc, nodes=getNodeSet(doc, "//row[1]/row"))
View(cta_month_rides_df2)
cta_month_rides_df3 <- xmlToDataFrame(doc, nodes=getNodeSet(doc, "//row[1]/*[name()!='row']"))
View(cta_month_rides_df3)
doc <- xmlParse("~/Documents/Data/Data_Videos/data/cta_monthly_l_rides_clean.xml")
cta_month_rides_df3 <- xmlToDataFrame(doc, nodes=getNodeSet(doc, "//row"))
doc <- xmlParse("~/Documents/Data/Data_Videos/data/cta_monthly_l_rides.xml")
cta_month_rides_df3 <- xmlToDataFrame(doc, nodes=getNodeSet(doc, "//row/row"))
View(cta_month_rides_df3)
33760-5
doc_preview <- xmlParse("~/Documents/Data/Data_Videos/data/cta_monthly_l_rides_preview.xml")
doc_preview
cta_month_rides_df3 <- cbind(XML:::xmlAttrsToDataFrame(doc, nodes=getNodeSet(doc, "//row/row"))
XML::xmlToDataFrame(doc, nodes=getNodeSet(doc, "//row/row"))
cta_month_rides_df3 <- cbind(XML:::xmlAttrsToDataFrame(doc, nodes=getNodeSet(doc, "//row/row")),
XML::xmlToDataFrame(doc, nodes=getNodeSet(doc, "//row/row"))
cta_month_rides_df3 <- cbind(XML:::xmlAttrsToDataFrame(doc, nodes=getNodeSet(doc, "//row/row")),
XML::xmlToDataFrame(doc, nodes=getNodeSet(doc, "//row/row")))
?XML:::xmlAttrsToDataFrame
cta_month_rides_df3 <- cbind(XML:::xmlAttrsToDataFrame(nodes=getNodeSet(doc, "//row/row")),
XML::xmlToDataFrame(nodes=getNodeSet(doc, "//row/row")))
cta_month_rides_df3 <- cbind(XML:::xmlAttrsToDataFrame(getNodeSet(doc, "//row/row")),
XML::xmlToDataFrame(nodes=getNodeSet(doc, "//row/row")))
View(cta_month_rides_df3)
doc2 <- xml2::read_xml("~/Documents/Data/Data_Videos/data/cta_monthly_l_rides_preview.xml")
vals <- xml_children(xml_find_all(doc2, "//row/row"))
library(XML)
library(xml2)
vals <- xml2::xml_children(xml_find_all(doc2, "//row/row"))
cta_month_rides_df2 <- setNames(data.frame(t(trimws(xml2::xml_text(vals)))), xml2::xml_name(vals))
rm(vals)
cta_month_rides_df2
vals <- xml2::xml_children(xml_find_all(doc2, "//row"))
cta_month_rides_df2 <- setNames(data.frame(t(trimws(xml2::xml_text(vals)))), xml2::xml_name(vals))
rm(vals)
cta_month_rides_df2
doc2
doc2$xml_document
doc2$node
doc2$doc$document
doc2$doc
doc2$doc$xml_document
doc2$doc[['xml_document']]
doc2
doc2 <- xml2::read_xml("~/Documents/Data/Data_Videos/data/cta_monthly_l_rides_preview.xml")
doc2
cta_month_rides_df <- cbind(XML:::xmlAttrsToDataFrame(getNodeSet(doc_preview, "//row/row")),
XML::xmlToDataFrame(nodes=getNodeSet(doc_preview, "//row/row")))
cta_month_rides_df <- cbind(XML:::xmlAttrsToDataFrame(getNodeSet(doc_preview, "//row/row")),
XML::xmlToDataFrame(nodes=getNodeSet(doc_preview, "//row/row")))
cta_month_rides_df
vals <- xml2::xml_children(xml_find_all(doc2, "row"))
cta_month_rides_df2 <- setNames(data.frame(t(trimws(xml2::xml_text(vals)))), xml2::xml_name(vals))
rm(vals)
cta_month_rides_df2
doc2 <- xml2::read_xml("~/Documents/Data/Data_Videos/data/cta_monthly_l_rides_preview.xml")
doc2
vals <- xml2::xml_children(xml_find_all(doc2, "row"))
cta_month_rides_df2 <- setNames(data.frame(t(trimws(xml2::xml_text(vals)))), xml2::xml_name(vals))
rm(vals)
cta_month_rides_df2
vals <- xml2::xml_children(xml_find_all(doc2, "//row/row"))
cta_month_rides_df2 <- setNames(data.frame(t(trimws(xml2::xml_text(vals)))), xml2::xml_name(vals))
rm(vals)
cta_month_rides_df2
vals <- xml2::xml_children(xml_find_all(doc2, "//row/row"))
cta_month_rides_df2 <- setNames(data.frame(t(trimws(xml2::xml_text(vals)))), xml2::xml_name(vals))
cta_month_rides_df2
View(vals)
View(vals)
vals <- xml2::xml_children(xml_find_all(doc2, "//row"))
cta_month_rides_df2 <- setNames(data.frame(t(trimws(xml2::xml_text(vals)))), xml2::xml_name(vals))
cta_month_rides_df2
View(vals)
View(cta_month_rides_df2)
vals <- xml2::xml_children(xml_find_all(doc2, "//row/row"))
vals <- xml2::xml_children(xml_find_all(doc2, "//row/row"))
cta_month_rides_df2 <- setNames(data.frame(t(trimws(xml2::xml_text(vals)))), xml2::xml_name(vals))
cta_month_rides_df2
View(cta_month_rides_df2)
View(vals)
# RETRIEVE data NODES
recs <- xml_find_all(new_xml, "//row/row")
# RETRIEVE data NODES
recs <- xml_find_all(doc2, "//row/row")
# BIND EACH CHILD TEXT AND NAME TO Player DFs
df_list <- lapply(recs, function(r)
data.frame(rbind(setNames(xml_text(xml_children(r)),
xml_name(xml_children(r)))),
stringsAsFactors = FALSE)
)
cta_month_rides_df2 <- do.call(rbind.data.frame, df_list)
View(cta_month_rides_df2)
# RETRIEVE data NODES
recs <- xml_find_all(doc2, "//row/row")
# BIND EACH CHILD TEXT AND NAME TO Player DFs
df_list <- lapply(recs, function(r)
data.frame(rbind(setNames(xml_text(xml_children(r)),
xml_name(xml_children(r)))),
stringsAsFactors = FALSE)
)
cta_month_rides_df2 <- do.call(rbind.data.frame, df_list)
cta_month_rides_df2
# RETRIEVE data NODES
recs <- xml2::xml_find_all(doc2, "//row/row")
# BIND EACH CHILD TEXT AND NAME
df_list <- lapply(recs, function(r) {
vals <- xml2::xml_children(r)
data.frame(rbind(setNames(xml2::xml_text(vals),
xml2::xml_name(vls))))
})
# RETRIEVE data NODES
recs <- xml2::xml_find_all(doc2, "//row/row")
# BIND EACH CHILD TEXT AND NAME
df_list <- lapply(recs, function(r) {
vals <- xml2::xml_children(r)
data.frame(rbind(setNames(xml2::xml_text(vals),
xml2::xml_name(vals))))
})
cta_month_rides_df2 <- do.call(rbind.data.frame, df_list)
cta_month_rides_df2
# RETRIEVE data NODES
recs <- xml2::xml_find_all(doc2, "//row/row")
# BIND EACH CHILD TEXT AND NAME
df_list <- lapply(recs, function(r) {
vals <- xml2::xml_children(r)
rbind(setNames(xml2::xml_text(vals),
xml2::xml_name(vals)))
})
cta_month_rides_df2 <- do.call(rbind.data.frame, df_list)
cta_month_rides_df2
# RETRIEVE data NODES
recs <- xml2::xml_find_all(doc2, "//row/row")
# BIND EACH CHILD TEXT AND NAME
df_list <- lapply(recs, function(r) {
vals <- xml2::xml_children(r)
rbind.data.frame(setNames(xml2::xml_text(vals),
xml2::xml_name(vals)))
})
cta_month_rides_df2 <- do.call(rbind.data.frame, df_list)
# RETRIEVE data NODES
recs <- xml2::xml_find_all(doc2, "//row/row")
# BIND EACH CHILD TEXT AND NAME
df_list <- lapply(recs, function(r) {
vals <- xml2::xml_children(r)
data.frame(rbind(setNames(xml2::xml_text(vals),
xml2::xml_name(vals)))
})
# RETRIEVE data NODES
recs <- xml2::xml_find_all(doc2, "//row/row")
# BIND EACH CHILD TEXT AND NAME
df_list <- lapply(recs, function(r) {
vals <- xml2::xml_children(r)
data.frame(rbind(setNames(xml2::xml_text(vals),
xml2::xml_name(vals))))
})
cta_month_rides_df2 <- do.call(rbind.data.frame, df_list)
cta_month_rides_df2
# RETRIEVE data NODES
recs <- xml2::xml_find_all(doc2, "//row/row")
# BIND EACH CHILD TEXT AND NAME
df_list <- lapply(recs, function(r) {
vals <- xml2::xml_children(r)
attrs <- xml2::xml_attr(r)
data.frame(rbind(setNames(cbind(attrs, xml2::xml_text(vals)),
c(xml2::xml_names(attrs), xml2::xml_name(vals)))))
})
?xml_attr
# RETRIEVE data NODES
recs <- xml2::xml_find_all(doc2, "//row/row")
# BIND EACH CHILD TEXT AND NAME
df_list <- lapply(recs, function(r) {
vals <- xml2::xml_children(r)
attrs <- xml2::xml_attrs(r)
data.frame(rbind(setNames(cbind(attrs, xml2::xml_text(vals)),
c(xml2::xml_names(attrs), xml2::xml_name(vals)))))
})
# RETRIEVE data NODES
recs <- xml2::xml_find_all(doc2, "//row/row")
# BIND EACH CHILD TEXT AND NAME
df_list <- lapply(recs, function(r) {
vals <- xml2::xml_children(r)
attrs <- xml2::xml_attrs(r)
data.frame(rbind(setNames(cbind(attrs, xml2::xml_text(vals)),
c(xml2::xml_name(attrs), xml2::xml_name(vals)))))
})
# RETRIEVE data NODES
recs <- xml2::xml_find_all(doc2, "//row/row")
# BIND EACH CHILD TEXT AND NAME
df_list <- lapply(recs, function(r) {
vals <- xml2::xml_children(r)
attrs <- xml2::xml_attrs(r)
data.frame(rbind(setNames(cbind(attrs, xml2::xml_text(vals)),
c(c("id", "uuid","position"), xml2::xml_name(vals)))))
})
cta_month_rides_df2 <- do.call(rbind.data.frame, df_list)
cta_month_rides_df2
# RETRIEVE data NODES
recs <- xml2::xml_find_all(doc2, "//row/row")
# BIND EACH CHILD TEXT AND NAME
df_list <- lapply(recs, function(r) {
vals <- xml2::xml_children(r)
attrs <- xml2::xml_attrs(r)
print(attrs)
data.frame(rbind(setNames(xml2::xml_text(vals)),
xml2::xml_name(vals)))
})
# RETRIEVE data NODES
recs <- xml2::xml_find_all(doc2, "//row/row")
# BIND EACH CHILD TEXT AND NAME
df_list <- lapply(recs, function(r) {
vals <- xml2::xml_children(r)
attrs <- xml2::xml_attrs(r)
print(attrs)
data.frame(rbind(setNames(xml2::xml_text(vals),
xml2::xml_name(vals))))
})
cta_month_rides_df2 <- do.call(rbind.data.frame, df_list)
cta_month_rides_df2
# RETRIEVE data NODES
recs <- xml2::xml_find_all(doc2, "//row/row")
# BIND EACH CHILD TEXT AND NAME
df_list <- lapply(recs, function(r) {
vals <- xml2::xml_children(r)
attrs <- xml2::xml_attrs(r)
data.frame(rbind(setNames(cbind(attrs, xml2::xml_text(vals)),
c(names(attrs), xml2::xml_name(vals)))))
})
cta_month_rides_df2 <- do.call(rbind.data.frame, df_list)
cta_month_rides_df2
# RETRIEVE data NODES
recs <- xml2::xml_find_all(doc2, "//row/row")
# BIND EACH CHILD TEXT AND NAME
df_list <- lapply(recs, function(r) {
vals <- xml2::xml_children(r)
attrs <- xml2::xml_attrs(r)
data.frame(rbind(setNames(c(attrs, xml2::xml_text(vals)),
c(names(attrs), xml2::xml_name(vals)))))
})
cta_month_rides_df2 <- do.call(rbind.data.frame, df_list)
cta_month_rides_df2
# RETRIEVE data NODES
recs <- xml2::xml_find_all(doc2, "//row/row")
# BIND EACH CHILD TEXT AND NAME
df_list <- lapply(recs, function(r) {
vals <- xml2::xml_children(r)
attrs <- xml2::xml_attrs(r)
data.frame(rbind(setNames(c(attrs, xml2::xml_text(vals)),
c(names(attrs), xml2::xml_name(vals)))),
check.names = FALSE)
})
cta_month_rides_df2 <- do.call(rbind.data.frame, df_list)
cta_month_rides_df2
# RETRIEVE data NODES
recs <- xml2::xml_find_all(doc2, "//row/row")
# BIND EACH CHILD TEXT AND NAME
df_list <- lapply(recs, function(r) {
attrs <- xml2::xml_attrs(r)
vals <- xml2::xml_children(r)
data.frame(rbind(setNames(c(attrs, xml2::xml_text(vals)),
c(names(attrs), xml2::xml_name(vals)))),
check.names = FALSE)
})
# COMBINE ALL DFS
cta_month_rides_df2 <- do.call(rbind.data.frame, df_list)
rm(df_list)
cta_month_rides_df2
# RETRIEVE data NODES
recs <- xml2::xml_find_all(doc2, "//row/row")
# BIND EACH CHILD TEXT AND NAME
df_list <- lapply(recs, function(r) {
attrs <- xml2::xml_attrs(r)
vals <- xml2::xml_children(r)
data.frame(rbind(setNames(c(attrs, xml2::xml_text(vals)),
c(names(attrs), xml2::xml_name(vals)))),
check.names = FALSE)
})
# COMBINE ALL DFS
cta_month_rides_df2 <- do.call(rbind.data.frame, df_list)
rm(recs, df_list)
cta_month_rides_df2
rm(vals)
doc_preview
library(XML)
library(jsonlite)
setwd("~/Documents/Data/Data_Videos/data")
# READ XML
doc <- xmlParse("cta_monthly_l_rides_preview.xml")
# CONVERT TO DATA FRAME
df <- xmlToDataFrame(nodes = getNodeSet(doc, "//row/row"))
# CONVERT TO JSON
x <- toJSON(df, pretty=TRUE)
# EXPORT TO FILE
fileConn<-file("cta_monthly_l_rides_preview.json")
writeLines(x, fileConn)
close(fileConn)
# READ JSON
cta_month_rides_df <- fromJSON("cta_monthly_l_rides_preview.json")
# READ JSON
cta_month_rides_df <- fromJSON("cta_monthly_l_rides_preview.json")
# READ JSON
cta_month_rides_df <- fromJSON("data/cta_monthly_l_rides_preview.json")
cta_month_rides_df
station_df <- data.frame(station = cta_month_rides_df[c("station_id", "stationame")],
month = cta_month_rides_df$month_beginning,
rides = cta_month_rides_df[grep("rides", names(cta_month_rides_df))])
str(station_df)
.sessionInfo()
sessionInfo()
# CONVERT TO JSON
x <- toJSON(station_df, pretty=TRUE)
# EXPORT TO FILE
fileConn<-file("cta_monthly_l_rides_nested.json")
writeLines(x, fileConn)
close(fileConn)
station_df <- list(station = cta_month_rides_df[c("station_id", "stationame")],
month = cta_month_rides_df$month_beginning,
rides = cta_month_rides_df[grep("rides", names(cta_month_rides_df))])
str(station_df)
# CONVERT TO JSON
x <- toJSON(station_df, pretty=TRUE)
# EXPORT TO FILE
fileConn<-file("cta_monthly_l_rides_nested.json")
writeLines(x, fileConn)
close(fileConn)
station_df <- lapply(1:nrow(cta_month_rides_df), function(i) {
list(station = cta_month_rides_df[i, c("station_id", "stationame")],
month = cta_month_rides_df$month_beginning[i],
rides = cta_month_rides_df[i, grep("rides", names(cta_month_rides_df))]
)
})
str(station_df)
# CONVERT TO JSON
x <- toJSON(station_df, pretty=TRUE)
# EXPORT TO FILE
fileConn<-file("cta_monthly_l_rides_nested.json")
writeLines(x, fileConn)
close(fileConn)
?toJSON
# CONVERT TO JSON
x <- toJSON(station_df, pretty=TRUE, auto_unbox=TRUE)
# EXPORT TO FILE
fileConn<-file("cta_monthly_l_rides_nested.json")
writeLines(x, fileConn)
close(fileConn)
station_df <- lapply(1:nrow(cta_month_rides_df), function(i) {
list(station = as.character(cta_month_rides_df[i, c("station_id", "stationame")]),
month = cta_month_rides_df$month_beginning[i],
rides = as.numeric(cta_month_rides_df[i, grep("rides", names(cta_month_rides_df)))]
)
})
str(station_df)
# CONVERT TO JSON
x <- toJSON(station_df, pretty=TRUE, auto_unbox=TRUE)
# EXPORT TO FILE
fileConn<-file("cta_monthly_l_rides_nested.json")
writeLines(x, fileConn)
close(fileConn)
station_df <- lapply(1:nrow(cta_month_rides_df), function(i) {
list(station = as.character(cta_month_rides_df[i, c("station_id", "stationame")]),
month = cta_month_rides_df$month_beginning[i],
rides = as.numeric(cta_month_rides_df[i, grep("rides", names(cta_month_rides_df))])
)
})
str(station_df)
# CONVERT TO JSON
x <- toJSON(station_df, pretty=TRUE, auto_unbox=TRUE)
# EXPORT TO FILE
fileConn<-file("cta_monthly_l_rides_nested.json")
writeLines(x, fileConn)
close(fileConn)
station_df <- lapply(1:nrow(cta_month_rides_df), function(i) {
list(station = as.character(cta_month_rides_df[i, c("station_id", "stationame")]),
month = cta_month_rides_df$month_beginning[i],
rides = setNames(as.numeric(cta_month_rides_df[i, grep("rides", names(cta_month_rides_df))]),
names(cta_rides_df)[grep("rides", names(cta_month_rides_df))])
)
})
station_df <- lapply(1:nrow(cta_month_rides_df), function(i) {
list(station = as.character(cta_month_rides_df[i, c("station_id", "stationame")]),
month = cta_month_rides_df$month_beginning[i],
rides = setNames(as.numeric(cta_month_rides_df[i, grep("rides", names(cta_month_rides_df))]),
names(cta_month_rides_df)[grep("rides", names(cta_month_rides_df))])
)
})
str(station_df)
# CONVERT TO JSON
x <- toJSON(station_df, pretty=TRUE, auto_unbox=TRUE)
# EXPORT TO FILE
fileConn<-file("cta_monthly_l_rides_nested.json")
writeLines(x, fileConn)
close(fileConn)
setNames(as.numeric(cta_month_rides_df[i, grep("rides", names(cta_month_rides_df))]),
names(cta_month_rides_df)[grep("rides", names(cta_month_rides_df))])setNames(as.numeric(cta_month_rides_df[i, grep("rides", names(cta_month_rides_df))]),
names(cta_month_rides_df)[grep("rides", names(cta_month_rides_df))])
setNames(as.numeric(cta_month_rides_df[1, grep("rides", names(cta_month_rides_df))]),
names(cta_month_rides_df)[grep("rides", names(cta_month_rides_df))])
station_df <- lapply(1:nrow(cta_month_rides_df), function(i) {
list(station = as.character(cta_month_rides_df[i, c("station_id", "stationame")]),
month = cta_month_rides_df$month_beginning[i],
rides = list(cta_month_rides_df[i, grep("rides", names(cta_month_rides_df))])
)
})
str(station_df)
station_df <- lapply(1:nrow(cta_month_rides_df), function(i) {
list(station = cta_month_rides_df[i, c("station_id", "stationame")],
month = cta_month_rides_df$month_beginning[i],
rides = cta_month_rides_df[i, grep("rides", names(cta_month_rides_df))]
)
})
str(station_df)
# CONVERT TO JSON
x <- toJSON(station_df, pretty=TRUE, auto_unbox=TRUE)
# EXPORT TO FILE
fileConn<-file("cta_monthly_l_rides_nested.json")
writeLines(x, fileConn)
close(fileConn)
station_df <- lapply(1:nrow(cta_month_rides_df), function(i) {
list(station = cta_month_rides_df[i, c("station_id", "stationame")],
month = cta_month_rides_df$month_beginning[i],
rides = list(average_weekday_rides = cta_month_rides_df$avg_weekday_rides[i])
)
})
str(station_df)
# CONVERT TO JSON
x <- toJSON(station_df, pretty=TRUE, auto_unbox=TRUE)
# EXPORT TO FILE
fileConn<-file("cta_monthly_l_rides_nested.json")
writeLines(x, fileConn)
close(fileConn)
station_df <- lapply(1:nrow(cta_month_rides_df), function(i) {
list(station = cta_month_rides_df[i, c("station_id", "stationame")],
month = cta_month_rides_df$month_beginning[i],
rides = as.list(cta_month_rides_df[i, grep("rides", names(cta_month_rides_df))])
)
})
str(station_df)
station_df <- lapply(1:nrow(cta_month_rides_df), function(i) {
list(station = as.list(cta_month_rides_df[i, c("station_id", "stationame")]),
month = cta_month_rides_df$month_beginning[i],
rides = as.list(cta_month_rides_df[i, grep("rides", names(cta_month_rides_df))])
)
})
str(station_df)
# CONVERT TO JSON
x <- toJSON(station_df, pretty=TRUE, auto_unbox = TRUE)
# EXPORT TO FILE
fileConn<-file("cta_monthly_l_rides_nested.json")
writeLines(x, fileConn)
close(fileConn)
readLines("data/cta_monthly_l_rides_preview.json")
cat(readLines("data/cta_monthly_l_rides_preview.json"))
readLines("data/cta_monthly_l_rides_preview.json")
cat(paste0(readLines("data/cta_monthly_l_rides_preview.json"), collapse="\n"))
cta_month_rides_df <- fromJSON("data/cta_monthly_l_rides_preview.json")
cta_month_rides_df
cat(paste0(readLines("data/cta_monthly_l_rides_nested.json"), collapse="\n"))
### PARSE
doc <- XML::xmlParse("~/Documents/Data/Data_Videos/data/cta_monthly_l_preview.xml")
### PARSE
doc <- XML::xmlParse("data/cta_monthly_l_preview.xml")
### PARSE
doc <- XML::xmlParse("data/cta_monthly_l_rides_preview.xml")
doc2 <- xml2::read_xml("ta/cta_monthly_l_rides_preview.xml")
### PARSE
doc <- XML::xmlParse("data/cta_monthly_l_rides_preview.xml")
doc2 <- xml2::read_xml("data/cta_monthly_l_rides_preview.xml")
doc
doc <- XML::xmlParse("data/cta_monthly_l_rides_nested.xml")
doc2 <- xml2::read_xml("data/cta_monthly_l_rides_nested.xml")
doc
